# Changelog

并入main分支时升版本号并更新，格式约定见 [docs/guides/release.md](docs/guides/release.md)。

## [2.0.2] - 2026-09-25

### LLM 运行时

- 思考强度控制：用途档可配思考深度，供应商级参数口袋承载各家方言，设了才随请求发送（ADR-033）。
- 缓存命中 token 分段计价：命中走折扣单价，不再按全价多记。
- 请求历史捕获思考 token 数，并完整保留原始用量单，解析缺陷可事后重放补数。
- 移除生成额度与请求硬超时，完整等待模型生成。
- 空响应统一进入重试入口，不再被静默吞掉。
- 保留单次调用的显式生成参数，不再被配置默认值覆盖。
- 无参工具 schema 兜底，避免严格端点 400。

### 主播 Agent

- 发言事件同步发出，修复直播时间线顺序倒挂。
- replyer 请求形状与历史窗口滞回优化，提升缓存命中。
- 决策上下文完整注入工具观察与观众画像。
- 保留完整场次对话与游戏叙事，长消息不再截断。
- 弹幕去重的兜底字段笔误修复。
- reply 工具补齐 category 分类。

### Minecraft Agent

- Minecraft 建造能力全链路调通：建筑设计循环、施工交接、交付门禁。
- 建筑设计拥有独立模型与任务预算，不挤占游戏决策资源。
- 任务历史按 token 预算集中整理，旧观察正文可按范围回读。
- 冻结分页证据校验取回；待应答决策与失败事实独立保存。
- 对接 Mod 场景校验与具名编辑；移除 max_steps 步数限制。
- 后台唤醒恢复原任务并延续步数。
- 区分待开工设计与后台运行任务。

### 观众画像与记忆

- 观众画像全链路：弹幕与礼物自动提取原子事实，压缩为长期画像并注入主播决策上下文（ADR-030）。
- 画像写入路径落地：事实逐条入库、按策略压缩、召回时携带原文。
- 付费明细：礼物、SC、舰长逐笔入库并带平台标价（ADR-029）。
- 平台身份复合键识别观众，跨场次累计画像与消费。
- 事实原文完整保留，召回可追溯。

### 虚拟形象（Avatar）

- VTS 的 v2 架构迁移彻底完成（ADR-023 至 028），其余平台待后续逐个调通。
- avatar 配置独立为第七配置文件，皮套域配置整体迁出工具域。
- 口型帧级通道：音频分接加共享分析器，多平台共享同一口型源。
- 字幕单一源落地，Warudo 字幕面退役。
- 虚拟形象工具面收敛 28 至 11 并立契约协议。
- 去平台化与自动情绪路径，主播域不再感知具体平台。
- 换模跟随：健康检查轮询模型名触发解析链重跑。
- 表情系统修复：双眉联合参数、idle 量纲缩放、停留期表情保鲜。
- VTS 未连接时降级登记并摘除工具。

### 工具系统

- 工具可见名单默认翻转为仅主播。
- 新增联网搜索与网页抓取工具。
- 连接型 Provider 生命周期接线补齐。
- 区分业务拒绝与执行故障。

### WebUI

- 请求历史"客户端"列正名为"用途"，历史旧值原样保留。
- 请求历史详情新增思考 token 数与原始用量单查看。
- 请求历史逐条缓存命中率；模型筛选补全量候选。
- 模型用量页新增上下文水位进度条。
- 直播时间线按实际时序渲染，思考段内联交织。
- 新增记忆管理页。
- 观众页补付费维度与礼物标价。
- 工具页支持手工填参调用；工具卡入参药丸渲染。
- 决策卡与发言卡新增上下文水位、缓存命中与表达请求统计胶囊。
- 干预入口按钮组化。
- 设置页卡片重排，名单改标签编辑。
- 模拟器页重设计对齐后端语义。
- 服务层分层重构（ADR-020）；图表迁移 ECharts。
- 事件时间戳全站统一毫秒（ADR-021）。

### 配置系统

- 列表型配置升为一等公民，支持整列表编辑（ADR-022）。
- 工具提供者 config 注册表校验与补全（ADR-027）。
- LLM 鉴权与重试字段标记为高级参数。

### 内部质量

- 统一日志门面 ModuleLogger，异常堆栈落盘。
- 吞异常路径全部补日志。
- 事件名统一以 CoreEvents 为唯一事实源。
- 全仓类型注解补全；依赖声明收敛为直接依赖。

### 升级注意

- 存储 SCHEMA_VERSION 升至 12：首次启动自动逐级迁移，旧库自动备份，无需手动干预。
- 配置新增第七文件 `avatar.toml`（首次启动自动生成）；工具域 avatar 段自动迁移。
- 请求历史列 `client_type` 改名 `profile_name`：迁移自动执行，历史旧值（llm/llm_fast 等旧分类）原样保留，不回填。
- 生成额度与请求硬超时相关配置项已清理，由完整等待语义取代。
- 发言事件契约必选化、情绪词表扩至 17 值——如有外挂消费方请注意对齐。

## [2.0.1] - 2026-09-19

### 采集器与事件系统

- 新增注意流采集器 `maicraft_attention`：持续观察游戏内受伤、死亡等身体事件，按伤害类型分类并叙事化后进入事件流。
- 新增 `game.body.*` 身体事件动态族。
- 常驻重连的同因失败不再重复刷屏。

### 游戏 Agent

- 游戏上报携带任务上下文与身体事件，游戏 Agent 增量读取身体事件。
- 任务台账改由注意流任务事件落账。

### 主播 Agent

- 委派规则进提示词：游戏内的行动就是主播的行动（第一人称话术），身体遭遇时不派活，派活要说清代价。
- 去掉委派与提示词中的 Minecraft 特例，框架层泛化为多游戏通用。
- 观察截断改为整段丢弃并自证，工具观察上限提升到 6144 字符。

### 工具系统

- 工具提供者常驻登记与工具集动态刷新；私有 MCP 断线自动重连，装配失败退避恢复。
- 工具页改注册表驱动：分类按提供者自声明动态发现，私有 MCP 开关直写配置。

### WebUI

- 新增观众数据三页：观众列表（搜索/排序/分页）、单观众档案（对话交织视图）、互动分析。
- LLM 用量页新增命中率统计与趋势图表。
- Agent 页信息呈现与操作收敛优化。
- 点击侧边栏版本号可查看更新日志。
- 配置端口被占用时自动回退后续端口并告警提示。
- 砍除开发者工具页：真实限流触发迁入直播控制台，EventBus 累计统计并入事件流页。
- 流程单域正名：API 与前端的 agenda 残留统一为 rundown。
- 修复：游戏上报事件补进直播时间线、侧边栏展示错误版本号、提示词预览空白。
- 删除 WebUI v1 残留死代码。

### 升级注意

- 默认配置新增并启用注意流采集器 `maicraft_attention`：已有配置在升级加载时自动补入该段（默认启用），不需要时在 WebUI 采集器页关闭。
- 无配置迁移钩子，存储 `SCHEMA_VERSION` 不变。

## [2.0.0] - 2026-09-13

v2 架构代际发布。设计推导见 [v2-architecture](docs/architecture/v2-architecture.md)，关键决策见 [ADR 清单](docs/decisions/README.md)。

### 整体架构

- `Input→Decision→Output` 三阶段流水线架构改为 `Agent`架构：主播 Agent 自我驱动，直播期间持续运行决策循环；游戏 Agent 命令驱动，收到指令才开工、完成即停；其余能力一律是工具，由 LLM 在决策中直接调用。
- 扩展点统一为 Agent 包、工具提供者与事件订阅，新增直播内容只需添加一个 Agent 包，局部用到的工具放在内部注册，避免v1架构加一个直播内容需要在三个地方加的问题。

### 主播 Agent

- 决策内核升级为 StreamerAgent，主播像一名真正的 Agent 一样自主工作，因MaiBot作为群聊Bot面对的群聊场景和直播场景有较大差异，不再依赖MaiBot作为决策核心。
- 过度设计的节目单简化重构为流程单功能，相当于一个直播大纲加定时提醒，在 WebUI 上可视化编辑并持久化到数据库，不再使用配置文件。
- 主动推进成为默认行为，没有观众也有节目推进。

### 游戏 Agent

- MinecraftAgent（AI 玩家）：主播派活唤醒任务循环，LLM 推理后调用私有 MCP 工具操作游戏，交付与求助经 game.report 回报主播，完成即停、空闲零消耗。
- TextAdvGameAgent（文字冒险）：auto 开关启停观察循环，VLM 读屏识别画面并驱动键鼠翻页，新剧情上报主播叙事，遇选项上报并等主播经工具点选。
- 新增游戏 = 新增一个 Agent 包：在 `src/agents/<name>/`（目录名即注册名）内实现决策循环、自有工具与提示词，加配置即接入，框架零改动。

### 工具

- 新增 MCP 工具源通道，外部 MCP server 的工具可接入 LLM 工具面，归属与可见范围按 Agent 限定。
- 屏幕感知从常驻采集改为按需调用，被问到才看屏幕。
- 工具故障熔断后探活自动恢复，外部连接可在 WebUI 手动重连。

### 事件系统

- 事件名统一为 CoreEvents 常量，EventBus 支持通配订阅，新增拦截器机制。
- 统一消息载荷退役，各事件载荷按域定义。
- 新增主播发言、游戏上报等业务事件；采集器统一为采集即发布事件。

### 配置与存储

- 配置收敛为 6 个文件，每文件带版本元数据，升级自动迁移并写回。
- 数据统一存入单一 SQLite 库，场次、决策与用量自动入库，表结构走版本化迁移。

### WebUI

- 重构为直播控制台，直播状态、Agent 控制与流程单编辑都在界面完成，设置页按配置 Schema 自动生成。

### 基础设施

- LLM 统一文本与视觉两种调用入口，支持流式输出；按 profile 选择模型并故障切换。
- TTS 引擎接口统一，从事件订阅改为直接调用。
- 字幕改为基础模块，支持多后端；修复字幕背景不透明和被裁剪问题。
- 记忆独立成模块，查询能力开放给 Agent。预留复杂记忆接口，添加简单记忆实现。
- 虚拟形象：表情与动作改由工具直接调用；新增 vrchat 独立包。
- 弹幕模拟器升级，与原本的mock采集器合并，支持 mock、回放、LLM 三种模式。

### 依赖与工程

- 移除 maim-message、dashscope 等依赖，fastmcp 升级至 3.x。
- 文档重组为 architecture、guides、decisions，关键决策落 ADR。

### 升级注意

- 配置布局变更：旧 `config/` 下 5 个文件不再读取。升级请先备份并移走旧 `config/`，首次启动自动生成新文件后重填模型 key、人设与开关。`[meta].version` 由系统维护，勿手工修改。
- 数据不自动迁移：旧节目单 `data/outlines/*.toml` 不再读取，流程单请在 WebUI 重新编排。
- 行为变化：主动发言默认开启；事件历史仅内存保存，重启即清；直播场次需显式开场；对外 MCP 服务端已移除，MCP 角色转为客户端。
- 依赖需 `uv sync` 重新安装（fastmcp 2.x → 3.x）。
- 开发者：`src/stages/` 与 pipeline、di、streaming、context 模块已移除；拉取后建议执行 `git add --renormalize .` 对齐行尾。

