# Amaidesu 配置文件元信息

[meta]
version = "1.0.0"  # 配置模板版本，用于自动更新检测

# Amaidesu 配置文件
#
# 📝 配置层次说明
# ================================
# 此配置文件是用户主配置，包含：
# 1. 全局配置（LLM、MaiCore连接等）
# 2. Provider启用列表
# 3. 常用参数覆盖（可选）
#
# 各Provider的详细配置在各自的 config.toml 中：
# - src/domains/{domain}/providers/{provider}/config.toml

# ========== 全局LLM配置 ==========

# 标准 LLM 配置（用于高质量任务）
[llm]
client = "openai"              # openai | ollama | anthropic
model = "Pro/deepseek-ai/DeepSeek-V3.2"
temperature = 0.2
api_key = "your-api-key"                    # 可选，如果不设置将使用环境变量
base_url = "https://api.siliconflow.cn/v1/" # 可选，用于自定义API端点
max_tokens = 1024
max_retries = 3                 # 可选，默认 3
retry_delay = 1.0               # 可选，默认 1.0

# 快速 LLM 配置（用于低延迟任务，如 Avatar 表情分析）
[llm_fast]
client = "openai"
model = "Qwen/Qwen3-8B"
temperature = 0.2
api_key = "your-api-key"
base_url = "https://api.siliconflow.cn/v1/"
max_tokens = 1024

# 视觉语言模型配置（用于图像理解任务）
[vlm]
client = "openai"
model = "zai-org/GLM-4.6V"
temperature = 0.3
api_key = "your-api-key"
base_url = "https://api.siliconflow.cn/v1/"
max_tokens = 1024

# 本地 Ollama 配置（可选）
[llm_local]
client = "ollama"
model = "llama3"
api_base = "http://localhost:11434/v1"
api_key = "sk-dummy"            # Ollama 不需要真实 API key

# ========== 共享服务配置 ==========

# DG-Lab 硬件控制服务
[dg_lab]
# DG-Lab API 地址
api_base_url = "http://127.0.0.1:8081"
# 默认强度 (0-200)
default_strength = 10
# 默认波形预设: small | medium | big | random
default_waveform = "big"
# 默认电击持续时间（秒）
shock_duration_seconds = 2.0
# HTTP 请求超时时间（秒）
request_timeout = 5.0
# 最大强度限制（安全保护）
max_strength = 50
# 是否启用安全限制（推荐启用）
enable_safety_limit = true

# ========== Amaidesu核心配置 ==========

[general]
# Amaidesu 在 MaiCore 中注册的平台标识符
platform_id = "amaidesu"

# ========== VTuber 人设配置 ==========
# 此配置段用于定义 VTuber 的性格和说话风格
# 被 LocalLLMDecisionProvider 等使用 LLM 的 Provider 引用
[persona]
# VTuber 名字
bot_name = "爱德丝"

# 性格描述（50字以内）
personality = "活泼开朗，有些调皮，喜欢和观众互动"

# 说话风格约束（指导 LLM 生成回复的风格）
style_constraints = "口语化，使用网络流行语，避免机械式回复，适当使用emoji"

# 用户称呼
user_name = "大家"

# 回复长度限制（字数）
max_response_length = 50

# 情感表达强度 (1-10, 1=平淡, 10=丰富)
emotion_intensity = 7

[maicore]
# MaiCore WebSocket 服务器地址
host = "127.0.0.1"
# MaiCore WebSocket 服务器端口
port = 8000
# token = "your_maicore_token_if_needed" # 如果 MaiCore 需要认证，取消注释并设置

# 讯飞 RTASR 实时识别凭据（可选）
[spark_rtasr]
app_id = ""
access_key_id = ""
access_key_secret = ""
# 可选：自定义 websocket URL（默认官方地址）
# ws_url = "wss://office-api-ast-dx.iflyaisol.com/ast/communicate/v1"

# HTTP回调服务器配置
[http_server]
enable = true
host = "127.0.0.1"
port = 8080
callback_path = "/maicore_callback"

# ========== 上下文管理器配置 ==========

[context]
# 上下文管理器配置
storage_type = "memory"  # 存储类型: memory 或 file
max_messages_per_session = 50  # 每个会话保留的最大消息数
max_sessions = 100  # 最大会话数
session_timeout_seconds = 3600  # 会话超时时间（秒）
enable_persistence = false  # 启用持久化（暂未实现）

# ========== 日志配置 ==========

[logging]
# 是否启用文件日志
enabled = true
# 日志格式: jsonl (JSON Lines) 或 text (纯文本)
format = "jsonl"
# 日志文件目录 (相对于项目根目录)
directory = "logs"
# 最小日志级别: TRACE | DEBUG | INFO | WARNING | ERROR | CRITICAL
level = "INFO"
# 文件轮转触发条件: "10 MB", "1 day", "00:00", "weekly", "monthly"
rotation = "10 MB"
# 日志保留时间: "7 days", "1 week", "1 month", "1 year"
retention = "7 days"
# 压缩格式: "zip", "gz", "tar", "" (禁用)
compression = "zip"
# 是否按会话分割日志文件（默认：false）
# 启用后，每次启动生成新文件（文件名包含时分秒，如 amaidesu_20260207_150423.jsonl）
# 禁用时，同一天多次启动会追加到同一文件（如 amaidesu_2026-02-07.jsonl）
split_by_session = false


# ========== 管道配置 ==========

[pipelines]
# TextPipeline 示例：限流
# [pipelines.rate_limit]
# priority = 100
# global_rate_limit = 100  # 全局每分钟最大消息数
# user_rate_limit = 10     # 每个用户每分钟最大消息数
# window_size = 60         # 滑动窗口大小（秒）

# TextPipeline 示例：限流
[pipelines.rate_limit]
priority = 100
enabled = true
global_rate_limit = 100
user_rate_limit = 10
window_size = 60

# TextPipeline 示例：相似消息过滤
[pipelines.similar_filter]
priority = 500
enabled = true
similarity_threshold = 0.85
time_window = 5.0

# === 输出管道（OutputPipeline）===
# OutputPipeline 示例：敏感词过滤
[pipelines.profanity_filter]
# OutputPipeline 配置
[pipelines.profanity_filter.output]
enabled = true
priority = 100
# 敏感词列表
words = ["测试脏话", "示例敏感词"]
replacement = "***"
# 可选：从文件加载
# wordlist_file = "data/profanity_words.txt"
# 是否过滤 TTS 文本（默认：true）
filter_tts = true
# 是否过滤字幕文本（默认：true）
filter_subtitle = true
# 是否区分大小写（默认：false）
case_sensitive = false
# 匹配到敏感词是否丢弃整个消息（默认：false）
drop_on_match = false

# ========== Provider配置 ==========

# === 输入Provider（Input Domain: 输入域） ===
[providers.input]
enabled = true
# 启用的输入Provider列表
enabled_inputs = [
    # 控制台输入（开发测试用）
    # "console_input",

    # B站弹幕输入
    # "bili_danmaku",           # 普通B站弹幕
    # "bili_danmaku_official",  # B站官方弹幕（不要和bili_danmaku同时启用）
    # "bili_danmaku_official_maicraft",  # B站官方弹幕（MaiCraft专用）

    # 语音输入
    # "stt",                   # 语音转文字（讯飞ASR + Silero VAD）

    # 其他输入
    # "mock_danmaku",           # 模拟弹幕
    # "read_pingmu",            # 读屏木
    # "mainosaba",              # Mainosaba读屏
]

# 常用参数覆盖（可选）
# [providers.input.overrides]
# bili_danmaku.room_id = "123456"

# === 输出Provider（Output Domain: 输出域） ===
[providers.output]
enabled = true
concurrent_rendering = true          # 是否并发渲染到多个Provider
error_handling = "continue"          # 错误处理策略: continue（继续） | stop（停止）
render_timeout = 10.0                # 单个Provider渲染超时（秒），0表示不限制

# 启用的输出Provider列表
enabled_outputs = [
    # 基础输出（推荐启用）
    "subtitle",     # 字幕
    "vts",          # VTS控制

    # TTS输出（选择一个或多个）
    # "tts",         # Edge TTS
    # "omni_tts",    # Omni TTS (GPT-SoVITS)

    # 虚拟形象输出
    # "avatar",      # 虚拟形象输出（支持VTS/VRChat/Live2D）

    # OBS控制
    # "obs_control", # OBS控制（文本显示、场景切换）

    # 其他输出
    # "sticker",     # 表情贴纸
    # "remote_stream",  # 远程流媒体输出
    # "warudo",      # Warudo控制
]

# ExpressionGenerator配置
[providers.output.expression_generator]
default_tts_enabled = true
default_subtitle_enabled = true
default_expressions_enabled = true
default_hotkeys_enabled = true

# 常用参数覆盖（可选）
# [providers.output.overrides]
# subtitle.font_size = 32
# subtitle.window_width = 1000
# vts.vts_host = "192.168.1.100"
# tts.voice = "zh-CN-YunxiNeural"

# === 决策Provider（Decision Domain: 决策域） ===
[providers.decision]
enabled = true
# 当前使用的决策Provider
active_provider = "maicore"
# 支持的决策Provider列表（用于运行时切换验证）
available_providers = [
    "maicore",        # MaiCore决策（默认，通过WebSocket连接MaiCore）
    "rule_engine",    # 规则引擎（本地规则匹配，低延迟）
    "local_llm",      # 本地LLM（直接调用LLM生成回复）
    "keyword_action", # 关键词动作决策（基于规则的关键词匹配）
    "maicraft",       # MaiCraft弹幕游戏决策
    "mock",           # 模拟Provider（用于测试）
]

# MaiCore DecisionProvider 配置
[providers.decision.maicore]
# MaiCore WebSocket 服务器地址（继承 [maicore] 配置）
# host = "127.0.0.1"
# port = 8000

# MaiCore DecisionProvider 子配置
[providers.decision.providers.maicore]
# Action 建议配置
# action_suggestions_enabled = false  # 是否启用 MaiBot Action 建议（默认关闭）
# action_confidence_threshold = 0.6   # 最低置信度阈值（0.0-1.0）
# action_cooldown_seconds = 5.0       # 同一 Action 的最小间隔（秒）
# max_suggested_actions = 3           # 每条消息最大建议数量

# LocalLLM DecisionProvider 配置
[providers.decision.local_llm]
# 降级模式（LLM调用失败时的行为）
fallback_mode = "simple"  # simple（返回原始文本） | echo（回声） | error（抛出异常）
# 注意：prompt_template 和 client_type 已移至 PromptManager 统一管理
# 提示词位置：src/prompts/templates/decision/local_llm_intent_parser.md.j2

# RuleEngine DecisionProvider 配置
[providers.decision.rule_engine]
# 规则文件路径
rules_file = "config/rules.json"
# 默认响应
default_response = "我不理解你的意思"
# 是否区分大小写
case_sensitive = false
# 匹配模式
match_mode = "any"  # any（任一关键词匹配） | all（所有关键词匹配）

# Mock DecisionProvider 配置
# [providers.decision.overrides]
# maicore.host = "192.168.1.100"
# local_llm.client_type = "llm_fast"

# ========== 新增 Provider 配置示例 ==========

# --- STTInputProvider: 语音转文字 ---
# [providers.input.inputs.stt]
# type = "stt"
#
# # 音频配置
# [providers.input.inputs.stt.audio]
# sample_rate = 16000
# channels = 1
# dtype = "int16"
# stt_input_device_name = "麦克风名称"  # 可选，留空使用默认设备
# use_remote_stream = false  # 使用远程音频流
#
# # VAD 配置
# [providers.input.inputs.stt.vad]
# enable = true
# vad_threshold = 0.5
# silence_seconds = 1.0
#
# # 讯飞 ASR 配置
# [providers.input.inputs.stt.iflytek_asr]
# host = "wss://istream-iflytek.xf-yun.com"
# path = "/v2/iat"
# appid = "your_appid"
# api_secret = "your_api_secret"
# api_key = "your_api_key"
# language = "zh_cn"
# domain = "iat"
#
# # 消息配置
# [providers.input.inputs.stt.message_config]
# user_id = "stt_user"
# user_nickname = "语音"

# --- BiliDanmakuOfficialInputProvider: B站官方弹幕 ---
# [providers.input.inputs.bili_danmaku_official]
# type = "bili_danmaku_official"
# id_code = "直播间ID代码"
# app_id = "应用ID"
# access_key = "访问密钥"
# access_key_secret = "访问密钥Secret"
# api_host = "https://live-open.biliapi.com"
# message_cache_size = 1000
#
# # 上下文过滤（可选）
# context_tags = ["游戏", "互动"]
#
# # 模板信息（可选）
# enable_template_info = false
# template_items = {}

# --- KeywordActionDecisionProvider: 关键词动作决策 ---
# [providers.decision.keyword_action]
# type = "keyword_action"
# global_cooldown = 1.0
# default_response = ""
#
# [[providers.decision.keyword_action.actions]]
# name = "微笑动作"
# enabled = true
# keywords = ["微笑", "smile", "😊"]
# match_mode = "anywhere"
# cooldown = 3.0
# action_type = "hotkey"
# action_params = { key = "smile" }
# priority = 50

# --- MaicraftDecisionProvider: 弹幕互动游戏决策 ---
# [providers.decision.maicraft]
# enabled = true
# factory_type = "log"  # log | mcp
# command_prefix = "!"
#
# [providers.decision.maicraft.command_mappings]
# chat = "chat"
# attack = "attack"

# --- AvatarOutputProvider: 虚拟形象输出 ---
# [providers.output.outputs.avatar]
# type = "avatar"
# adapter_type = "vts"  # vts | vrchat | live2d
#
# # VTS 特定配置
# vts_host = "127.0.0.1"
# vts_port = 8000
#
# # VRChat 特定配置
# vrc_host = "127.0.0.1"
# vrc_in_port = 9001
# vrc_out_port = 9000
# enable_server = false

# --- GPTSoVITSOutputProvider: GPT-SoVITS 语音合成 ---
# [providers.output.outputs.gptsovits]
# type = "gptsovits"
# host = "127.0.0.1"
# port = 9880
# ref_audio_path = "path/to/reference.wav"
# prompt_text = "参考文本"
# text_language = "zh"
# prompt_language = "zh"
# top_k = 20
# top_p = 0.6
# temperature = 0.3
# speed_factor = 1.0
# streaming_mode = true
# sample_rate = 32000
# output_device_name = "扬声器名称"

# --- ObsControlOutputProvider: OBS 控制 ---
# [providers.output.outputs.obs_control]
# type = "obs_control"
# host = "localhost"
# port = 4455
# password = "your_password"  # 可选
# text_source_name = "text"
# typewriter_enabled = false
# typewriter_speed = 0.1
# typewriter_delay = 0.5
# test_on_connect = true

# --- StickerOutputProvider: 贴纸输出 ---
# [providers.output.outputs.sticker]
# type = "sticker"
# sticker_size = 0.33
# sticker_rotation = 90
# sticker_position_x = 0.0
# sticker_position_y = 0.0
# image_width = 256
# image_height = 256
# cool_down_seconds = 5.0
# display_duration_seconds = 3.0

# --- RemoteStreamOutputProvider: 远程流媒体输出 ---
# [providers.output.outputs.remote_stream]
# type = "remote_stream"
# server_mode = true
# host = "0.0.0.0"
# port = 8765
# audio_sample_rate = 16000
# audio_channels = 1
# audio_format = "int16"
# audio_chunk_size = 1024
# image_width = 640
# image_height = 480
# image_format = "jpeg"
# image_quality = 80
# reconnect_delay = 5
# max_reconnect_attempts = -1  # -1 表示无限
