# src/pipelines/throttle/config-template.toml

[throttle]
# 是否启用此管道 (注意：此项在主配置中也需要设置优先级)
enabled = true

# 全局消息频率限制（每分钟最大消息数量）
global_rate_limit = 100

# 每个用户消息频率限制（每分钟最大消息数量）
user_rate_limit = 10

# 时间窗口大小（秒）
window_size = 60
