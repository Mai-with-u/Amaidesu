"""
渲染参数数据类 - 向后兼容性重导出层

══════════════════════════════════════════════════════════════════════════════
⚠️  架构迁移说明 (Architecture Migration Notice)
══════════════════════════════════════════════════════════════════════════════

此文件是向后兼容的重导出层（Compatibility Shim）。

类型定义已从 Output Domain 迁移到 Modules 层，以遵循3域架构原则：
- 原位置: src/domains/output/parameters/render_parameters.py
- 新位置: src/modules/types/render_parameters.py

迁移原因 (Migration Rationale):
1. RenderParameters 被 ExpressionGenerator (Modules层) 使用
2. 导致 Modules 层依赖 Output Domain，违反分层原则
3. 将类型提升到 Modules 层，保持单向依赖：Modules ← Output

══════════════════════════════════════════════════════════════════════════════
📋 使用指南 (Usage Guidelines)
══════════════════════════════════════════════════════════════════════════════

✅ 推荐（新代码）:
    from src.modules.types import ExpressionParameters, RenderParameters

⚠️  兼容（旧代码）:
    from src.domains.output.parameters import RenderParameters  # 仍然有效

❌ 不推荐（但仍然支持）:
    from src.domains.output.parameters.render_parameters import RenderParameters

══════════════════════════════════════════════════════════════════════════════
🔄 迁移计划 (Migration Plan)
══════════════════════════════════════════════════════════════════════════════

阶段 1（当前）: 重导出层，保持向后兼容
    - 旧导入路径仍然有效
    - 新代码使用 Modules 层导入

阶段 2（未来）: 标记为废弃
    - 添加 DeprecationWarning
    - 给出迁移期限

阶段 3（最终）: 移除此文件
    - 所有代码迁移到新导入路径
    - 删除向后兼容层

══════════════════════════════════════════════════════════════════════════════
"""

# 向后兼容：重导出从 Modules 层的类型定义
# 实际类型定义在: src/modules/types/render_parameters.py
from src.modules.types import ExpressionParameters, RenderParameters

__all__ = ["ExpressionParameters", "RenderParameters"]
