# Mock Providers Plugin 配置
# 模拟插件，为每一层提供模拟Provider

[general]
# 是否启用插件
enabled = true

# 是否立即开始数据采集
start_immediately = true

# === 启用的Provider ===
# 可以通过禁用某些Provider来测试特定层级
enable_input = true    # 模拟弹幕输入
enable_decision = true # 模拟AI决策
enable_output = true   # 模拟TTS和字幕输出

# === 输入Provider配置 ===
[general.input]
# 模拟弹幕发送间隔（秒）
send_interval = 1.0

# 随机间隔范围（秒）
min_interval = 0.5
max_interval = 2.0

# === 决策Provider配置 ===
[general.decision]
# 模拟AI思考延迟（秒）
response_delay = 0.5

# 是否启用关键词匹配
enable_keyword_match = true

# 是否添加随机变化（语气词等）
add_random_variation = true

# === 输出Provider配置 ===

# 模拟TTS输出
[general.output.tts]
# 模拟TTS播放延迟（秒）
speak_delay = 0.0

# 是否显示时间戳
show_timestamp = true

# 输出前缀
prefix = "🔊 TTS"

# 模拟字幕输出
[general.output.subtitle]
# 字幕显示时长（秒）
display_duration = 2.0

# 是否显示边框
show_border = true

# 边框字符
border_char = "═"

# 字幕宽度
width = 60
